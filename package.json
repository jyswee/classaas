{
  "name": "classaas-cli",
  "version": "0.1.0",
  "description": "ClassaaS CLI — teach without the stack, from the command line",
  "bin": {
    "caas": "bin/cli.js"
  },
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\""
  },
  "engines": {
    "node": ">=18"
  },
  "author": "Tyga.Cloud Ltd",
  "license": "UNLICENSED"
}
